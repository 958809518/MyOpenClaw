# 🎯 多模态快速参考卡

## 三种输入模态对比

```
┌──────────┬────────────────┬───────────┬──────────────┬────────────────┐
│  模态    │   激活方式     │  延迟     │  准确率      │  最佳场景      │
├──────────┼────────────────┼───────────┼──────────────┼────────────────┤
│ 📝 文本  │ 默认激活       │ 0ms       │ 100%         │ 精准控制       │
│ 🎤 语音  │ 点击"语音"标签 │ 2-3s      │ 85-95%       │ 自然交互       │
│ 📷 图像  │ 点击"图像"标签 │ 待实现    │ 待实现       │ 计算机视觉应用 │
└──────────┴────────────────┴───────────┴──────────────┴────────────────┘
```

---

## 📝 **文本模态使用**

### 步骤
```
1. 在输入框中输入指令（默认模态）
2. 点击 [生成] 按钮
```

### 常用指令示例
```
生成一个黄塔              → 单个黄色高塔
生成一个白塔              → 单个白色高塔  
生成高密度住宅区          → 100个随机建筑
生成商业区                → 50个随机建筑（低密度）
```

### 代码触发
```javascript
// 系统自动发送
POST /api/generate-city
{
  "intent": "生成一个黄塔",
  "modality": "text",
  "constraints": { "density": "high" }
}
```

---

## 🎤 **语音模态使用**

### 前置条件
- 有工作的麦克风
- 浏览器支持 Web Speech API（Chrome、Edge、Safari）
- 已授予麦克风权限

### 步骤
```
1. 点击 GUI 面板中的 [语音] 标签
2. 系统显示 "🎤 开始监听语音..."
3. 对着麦克风说出指令（例如："生成一个黄塔"）
4. 完成说话后，系统显示识别结果
5. 点击 [生成] 按钮
```

### 工作流程
```
用户说话 → Web Speech API 识别 
    ↓
自动填入输入框 → 用户点击生成 
    ↓
后端收到 modality='voice'
```

### 调试
| 问题 | 解决方案 |
|------|--------|
| 识别失败 | 检查麦克风连接，说话更清晰 |
| 无法启动 | 确认浏览器支持，检查网络 |
| 权限被拒 | 在浏览器设置中允许麦克风 |

---

## 📷 **图像模态使用**

### 前置条件
- 有工作的摄像头（笔记本内置或外接 USB）
- 浏览器支持 getUserMedia API
- 已授予摄像头权限

### 当前状态 ⚠️
```
✅ 摄像头流已实现（直播预览功能待开发）
❌ 图像→意图分析功能（需后端 AI 集成）
❌ 自动识别和生成（计划中）
```

### 使用步骤（当前）
```
1. 点击 GUI 面板中的 [图像] 标签
2. 系统显示 "✓ 摄像头已启用"
3. 手动输入与视频内容相关的指令
4. 点击 [生成] 按钮
```

### 后续计划
```javascript
// 正在开发的功能：
POST /api/generate-city
{
  "intent": "", // 自动从图像提取
  "modality": "image",
  "image_data": "data:image/png;base64,...", // 视频帧
  "constraints": { "density": "auto" }
}
```

---

## 🔄 **模态切换流程**

```
┌─────────────┐
│   系统启动   │
└──────┬──────┘
       │
    ┌──▼──┐
    │ 初始化 Speech Recognition
    │ 请求权限（麦克风 + 摄像头）
    └──┬──┘
       │
    ┌──▼─────────────────────┐
    │ currentModality = 'text'│  (默认)
    └──┬─────────────────────┘
       │
     ┌─┴─────────────┬──────────────┐
     │               │              │
  ┌──▼──┐      ┌──────▼───┐   ┌──────▼────┐
  │文本  │      │ 语音     │   │ 图像      │
  │(默认)│      │(需点击)  │   │(需点击)   │
  └──┬──┘      └──────┬───┘   └──────┬────┘
     │                │              │
  输入框          speechRecognition  getUserMedia
   输入           .start()            ({video:...})
     │                │              │
  点击生成          自动提交          手动输入指令
  └─────┬──────────┬──────────┬──────┘
        │          │          │
        └──┬───────┴──────────┘
           │
        POST /api/generate-city
           ▼
        后端处理 + 3D 渲染
```

---

## 🧠 **后端意图解析逻辑**

```javascript
// server/aiMock.js 中的当前实现

if (intent.match(/塔|tower/) && intent.match(/黄|黄色/)) {
  // 生成单个黄塔
  return { structures: [{ 
    id: "tower_0", 
    type: "tower", 
    height: 40, 
    color: 0xf1c40f 
  }]}
}

if (intent.match(/塔|tower/) && intent.match(/白|白色/)) {
  // 生成单个白塔
  return { structures: [{ 
    id: "tower_0", 
    type: "tower", 
    height: 40, 
    color: 0xffffff 
  }]}
}

// 其他情况：生成随机建筑群
return { structures: generateRandomBuildings(count) }
```

---

## 🎨 **UI 元素说明**

```
┌─────────────────────────────────────────────┐
│ 智慧城市生成系统                              │  ← 标题
├─────────────────────────────────────────────┤
│ ⟨  [折叠/展开] 按钮                          │  ← 头部控制
├─────────────────────────────────────────────┤
│ 📝 文本 | 🎤 语音 | 📷 图像                   │  ← 模态标签
├─────────────────────────────────────────────┤
│                                             │
│  生成意图 (多模态输入)                        │
│  ┌─────────────────────────────────┐        │
│  │ 例如：生成高密度住宅区...        │        │  ← 文本输入框
│  └─────────────────────────────────┘        │
│              [生成]                          │  ← 生成按钮
│                                             │
├─────────────────────────────────────────────┤
│ 约束条件：○ 高密度  ● 低密度                  │  ← 密度选择
├─────────────────────────────────────────────┤
│ 系统日志 ▼                                   │
│ ├─ 系统初始化完成...                        │
│ ├─ 加载 dVAE 编码器集群...                  │
│ ├─ 正在调用多模态大模型...                  │
│ └─ 推理完成。耗时：1326ms                   │
├─────────────────────────────────────────────┤ ← 可调整
│ 🎯 性能统计 (45 FPS, 建筑:1)               │    (右下角 ↗)
└─────────────────────────────────────────────┘
      ▲ 拖动头部可移动面板
```

---

## 📊 **API 请求响应对照表**

### 请求格式
```json
{
  "intent": "用户输入或语音识别结果",
  "modality": "text | voice | image",
  "constraints": {
    "density": "low | high"
  }
}
```

### 成功响应（200 OK）
```json
{
  "status": "success",
  "compute_node": "Domestic-NPU-Cluster-01",
  "latency": 1326,
  "data": {
    "tokens_used": 1024,
    "model_version": "MLLM-City-v1.0",
    "structures": [
      {
        "id": "tower_0",
        "type": "tower",
        "height": 40,
        "position": { "x": 0, "y": 0, "z": 0 },
        "color": 16756495
      }
    ]
  }
}
```

### 错误响应
```json
{
  "status": "error",
  "message": "Invalid intent format",
  "code": "VALIDATION_ERROR"
}
```

---

## 🚀 **性能提示**

| 操作 | 建议 | 原因 |
|------|------|------|
| 文本输入 | ✅ 立即提交 | 0ms 延迟 |
| 语音识别 | ⏳ 等待 2-3s | 网络识别时间 |
| 视频分析 | ⚠️ 待实现 | 需 AI 后端支持 |
| 多次生成 | ✅ 可连续使用 | 后端已优化缓存 |

---

## 💻 **开发者扩展点**

### 添加新的意图识别模式
编辑 `server/aiMock.js`：
```javascript
// 添加新的正则模式
if (intent.match(/红塔|tower.*red/)) {
  return { structures: [buildTower('red', 'tower_red')] }
}
```

### 自定义模态处理
编辑 `public/main.js`：
```javascript
// 添加新模态
let currentModality = 'text|voice|image|custom';

async function startCustomModality() {
  log("🔮 自定义模态已激活");
  // 实现自定义逻辑
}
```

### 集成真实 MLLM 服务
替换 `server/aiMock.js`：
```javascript
const response = await fetch('https://api.together.ai/inference', {
  method: 'POST',
  body: JSON.stringify({ 
    prompt: intent,
    model: 'meta-llama/Llama-2-7b-chat'
  })
});
```

---

## 🆘 **常见问题排查**

### Q: 点击语音标签后没有反应？
A: 
1. 检查浏览器是否支持 Web Speech API（Chrome/Edge 最佳）
2. 确认麦克风已连接并工作
3. 查看浏览器控制台（F12）获取错误信息

### Q: 黄塔还是没有显示？
A:
1. 确保输入的是 "生成一个黄塔" 或相关关键词
2. 检查后端指令处理逻辑（`aiMock.js`）
3. 查看浏览器 3D 渲染器日志

### Q: 如何切换回文本模态？
A:
1. 点击 "文本" 标签
2. 直接在输入框输入文本
3. 点击 [生成]

### Q: 图像模式什么时候可用？
A:
- 摄像头访问：现在可用 ✅
- 图像→意图分析：需后端 AI 集成（计划中）

---

## 📞 **快速命令参考**

```bash
# 启动服务器
npm start

# 开发模式（自动重载）
npm run dev

# 运行多模态测试
node tools/test-multimodal.js

# 查看完整文档
cat MULTIMODAL_GUIDE.md
```

---

**祝您的多模态应用运行顺利！🎉**
