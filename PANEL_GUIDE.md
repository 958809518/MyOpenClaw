# 🎛️ 面板缩放与管理指南

智慧城市系统提供了完整的 **浮动面板管理功能**，包括收起、展开、拖动和缩放。

---

## 🎯 核心功能一览

| 功能 | 操作 | 效果 |
|------|------|------|
| 📌 **显示/隐藏** | 点击 ⟨/⟩ 按钮 | 展开/收起面板内容 |
| 🖱️ **拖动移动** | 拖动面板头部 | 在屏幕任意位置移动面板 |
| 📏 **缩放调整** | 拖动右下角把手（↗） | 改变面板宽度和高度 |
| 💾 **状态记忆** | 自动保存 | 缩放和位置会被记住 |

---

## 🎮 详细使用方法

### 1️⃣ **收起面板** (隐藏所有内容)

**目的：** 获得最大 3D 场景视野

**操作步骤：**
```
1. 找到面板左上角的 ⟨ 按钮
2. 点击 ⟨ 按钮
3. 面板收缩为 50×50px 的小图标
4. 只显示切换按钮和右下角的缩放把手 ↗
```

**视觉效果：**
```
展开时：                    收起时：
┌─────────────┐            ┌──┐
│ 智慧城市 AI  │            │⟩ │
│ [所有内容]  │     →       │  │ ↗
│ [完整面板]  │            └──┘
└─────────────┘
```

---

### 2️⃣ **展开面板** (恢复所有内容)

**目的：** 重新显示所有控制选项和日志

**操作步骤：**
```
1. 点击收起的面板上的 ⟩ 按钮
2. 面板立即展开到之前的大小
3. 所有内容和控制选项恢复
4. 保持之前的自定义尺寸（如果你缩放过）
```

**自动恢复功能：**
✨ 系统会记住以下信息：
- 面板的宽度和高度（收起前）
- 面板的位置（X, Y 坐标）
- 展开时会精确恢复这些参数

---

### 3️⃣ **拖动面板** (移动位置)

**目的：** 将面板放在屏幕的任意位置

**操作步骤：**
```
1. 将鼠标放在面板的顶部（头部区域）
2. 按住鼠标左键（或触控）
3. 拖动到新位置
4. 松开鼠标完成移动
```

**支持范围：**
- ✅ 整个浏览器窗口范围内
- ✅ 可以移到屏幕顶部、底部、左侧、右侧
- ✅ 移动会自动保存（展开/收起时保持新位置）

**提示：**
💡 在"拖动"状态下，面板会显示半透明效果，便于查看后面的内容

---

### 4️⃣ **缩放面板** (调整大小)

**目的：** 自定义面板的显示尺寸

**操作步骤：**
```
1. 找到面板右下角的 ↗ 把手（蓝色方块）
2. 将鼠标放在把手上（会显示亮蓝色效果）
3. 按住鼠标左键
4. 向右下拖动 → 面板变大
5. 向左上拖动 → 面板变小
6. 松开鼠标完成缩放
```

**尺寸限制：**
| 参数 | 最小值 | 最大值 |
|------|--------|--------|
| 宽度 | 240px | 800px |
| 高度 | 150px | 600px |

**视觉反馈：**
- 🔵 把手默认：蓝色边框、透明背景
- 🔶 把手悬停：亮蓝色发光效果、放大 1.15×
- 🟢 把手拖动：深蓝色、放大 1.05×

---

### 5️⃣ **把手始终可见** (设计优势)

**特点：**
```
即使面板收起，↗ 把手仍然可见
这样你可以：
  1. 在面板收起时快速展开它
  2. 随时调整面板大小
  3. 不用为把手隐藏而烦恼
```

**把手位置：**
- 展开时：面板右下角外侧 14px
- 收起时：仍在原位置，保持可点击

---

## 🔄 完整操作流程示例

### 场景：创建完美的工作布局

```
初始状态：
  面板位置：(20px, 20px)
  面板大小：320×400px
  状态：展开 ✅

步骤 1：调整到合适大小
  - 拖动 ↗ 把手，缩放到 500×500px
  - 系统自动保存新尺寸 💾

步骤 2：移动到最佳位置
  - 拖动面板头部移到屏幕右侧
  - 新位置：(1000px, 50px)
  - 系统自动保存新位置 💾

步骤 3：开始使用
  - 在展开状态下输入指令、查看日志
  - 需要更多 3D 视野时，点击 ⟨ 收起面板
  - 需要控制时，点击 ⟩ 展开面板

步骤 4：以后的每次打开
  - 页面刷新后，面板会在上次的位置和大小
  - 不需要重新调整 ⚡
```

---

## 💡 高级技巧

### 技巧 1：快速收起以获得最大视野
```
快捷操作：
1. 点击 ⟨ 收起面板
2. 整个 3D 场景展开全屏
3. 看到缩放把手 ↗ 时点击可快速恢复
```

### 技巧 2：多个监控面板
```
如果你需要监控多个参数：
1. 缩小面板宽度到 240px（最小值）
2. 将两个或多个缩小的面板放在屏幕不同位置
3. 每个面板可独立操作和调整大小
```

### 技巧 3：竖屏模式
```
对于竖屏或狭长窗口：
1. 缩放面板为 240×150px（最小尺寸）
2. 沿屏幕边缘放置面板
3. 3D 场景占据大部分空间
```

### 技巧 4：演示模式
```
演示给他人时：
1. 点击 ⟨ 收起面板
2. 让 3D 场景占满整个屏幕
3. 需要调整时，点击 ↗ 把手，会自动展开
```

---

## 🔧 键盘快捷键 (规划中)

| 快捷键 | 功能 | 状态 |
|--------|------|------|
| <kbd>H</kbd> | 切换隐藏/显示 | ⏳ 计划中 |
| <kbd>R</kbd> | 重置为默认位置 | ⏳ 计划中 |
| <kbd>F</kbd> | 全屏 3D 视图 | ⏳ 计划中 |

---

## 🐛 常见问题与解决方案

### Q1: 缩放把手在哪里？
```
A: ↗ 把手在面板的右下角（外侧 14px）
   - 看起来像一个蓝色小方块
   - 鼠标靠近时会发出蓝光
   - 即使面板收起也保持可见
```

### Q2: 面板收起后怎么恢复？
```
A: 三种方法恢复：
   1. 点击 ⟩ 按钮（在收起的小面板上）
   2. 点击 ↗ 把手会自动展开
   3. 刷新页面后会自动记住状态
```

### Q3: 可以无限缩小吗？
```
A: 不行，有最小限制：
   - 最小宽度：240px
   - 最小高度：150px
   - 防止内容无法显示
```

### Q4: 缩放状态会保存吗？
```
A: 会的！系统会持续记忆：
   - 收起展开状态中：✅ 自动保存尺寸
   - 页面刷新后：✅ 保持上次的尺寸和位置
   - 浏览器缓存：⏳ 计划支持本地存储
```

### Q5: 在收起状态下能生成吗？
```
A: 不能直接操作，但可以：
   1. 点击 ↗ 把手快速展开
   2. 输入指令并点击生成
   3. 或点击 ⟨ 收起再隐藏面板
```

---

## 📊 面板状态指示

### 展开状态 🟢
```
✅ 所有内容可见
✅ 标题、输入框、按钮都可用
✅ 日志面板显示完整信息
✅ 密度选择器可操作
```

### 收起状态 🔵
```
✅ 面板压缩为 50×50 小图标
✅ 切换按钮 ⟩ 仍然可见
✅ 缩放把手 ↗ 保持可交互
✅ 内容隐藏，节省屏幕空间
```

### 拖动状态 🟡
```
✅ 面板半透明效果
✅ 显示新位置预览
✅ 内容跟随鼠标移动
✅ 松开后自动保存新位置
```

### 缩放状态 🟠
```
✅ 把手显示明亮蓝光
✅ 面板边界实时更新
✅ 宽高同时调整
✅ 松开后保存新尺寸
```

---

## 🚀 最佳实践

### ✅ 推荐做法
- 频繁切换收起/展开以优化屏幕利用
- 根据任务类型调整面板大小
- 定期整理面板位置
- 在演示时收起面板获得最佳视觉效果

### ❌ 不推荐做法
- 将面板拖出屏幕可见范围（可能丢失）
- 无节制地扩大面板到最大（影响 3D 性能）
- 快速连续点击收起/展开（可能产生闪烁）

---

## 📞 反馈与支持

遇到面板布局问题？
- 查看本指南的常见问题部分
- 检查浏览器控制台是否有错误（F12）
- 尝试刷新页面重置状态

---

**现在就开始使用吧！🎊**

通过灵活的面板管理，您可以创造最适合自己的工作环境。
