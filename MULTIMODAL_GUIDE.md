# 🎬 多模态输入使用指南

## 概述

智慧城市生成系统支持三种输入模态：**文本**、**语音**、**图像/视频**。用户可根据场景选择最适合的输入方式。

---

## 📝 **模态 1：文本输入（推荐）**

### 使用步骤
1. 点击 GUI 面板中的 **"文本"** 标签（默认激活）
2. 在输入框中输入生成意图，例如：
   - `生成一个黄塔`
   - `生成一个白塔`
   - `生成高密度住宅区`
3. 点击 **"生成"** 按钮

### 示例指令
| 指令 | 效果 |
|------|------|
| `生成一个黄塔` | 生成单个黄色高塔（height=40） |
| `生成一个白塔` | 生成单个白色高塔（height=40） |
| `生成住宅区` | 生成50个随机建筑（低密度） |
| `生成城市` | 生成100个随机建筑（高密度） |

---

## 🎤 **模态 2：语音输入**

### 前置条件
- 浏览器支持 Web Speech API（Chrome、Edge、Safari）
- 已授予麦克风权限

### 使用步骤
1. 点击 GUI 面板中的 **"语音"** 标签
2. 系统自动开始监听，界面显示：`🎤 开始监听语音...`
3. 朝麦克风清晰地说出指令，例如：
   - "生成一个黄塔"
   - "生成城市"
4. 完成说话后，系统自动识别并显示：`🎤 识别结果: "..."`
5. 点击 **"生成"** 按钮

### 注意事项
- ⚠️ 需要稳定的网络连接（离线可能不可用）
- 🌐 某些地区可能需要科学上网
- 🔊 说话时避免背景噪音
- ⏱️ 识别通常在 2-3 秒内完成

### 支持语言
- 简体中文（zh-CN）
- 英文（可通过修改代码支持）

---

## 📷 **模态 3：图像/视频输入**

### 前置条件
- 浏览器支持 getUserMedia API
- 已授予摄像头权限
- 拥有一台工作的摄像头/USB 摄像头

### 使用步骤
1. 点击 GUI 面板中的 **"图像"** 标签
2. 系统自动启用摄像头，界面显示：`✓ 摄像头已启用`
3. 当前实现为**占位符**（真实图像分析需后端 AI 支持）
4. 手动输入描述或使用语音输入结合视频
5. 点击 **"生成"** 按钮

### 功能路线图
- [ ] 实时视频预览（HTML5 Canvas）
- [ ] 关键帧捕捉
- [ ] 图像分析 API 集成（调用真实 MLLM）
- [ ] 图像特征提取与生成指令自动化

---

## 🎯 **权限管理**

### 首次使用
- 系统在初始化时自动请求麦克风 + 摄像头权限
- 允许权限才能使用对应模态

### 权限设置
| 模态 | 权限需求 | 通知 |
|------|---------|------|
| 文本 | 无 | ✓ 始终可用 |
| 语音 | 麦克风 | 🎤 首次使用会弹出权限请求 |
| 图像 | 摄像头 | 📷 首次使用会弹出权限请求 |

### 撤销权限（浏览器各异）
- **Chrome**：地址栏左侧摄像头/麦克风图标 → 允许/阻止
- **Firefox**：菜单 → 偏好设置 → 隐私与安全 → 权限
- **Safari**：系统设置 → 隐私与安全

---

## 💡 **最佳实践**

### 场景 1：快速原型设计
```
推荐：文本输入
原因：最可靠、最快、无权限阻拦
```

### 场景 2：演示与展示
```
推荐：语音输入
原因：交互感强、演示效果好
注意：预先测试麦克风工作状态
```

### 场景 3：数据驱动生成
```
推荐：图像输入 + API 后端
原因：基于视觉内容自动生成（需后端支持）
```

---

## 🔧 **故障排查**

### 问题 1：语音识别不工作
```
症状：说话后无反应，无识别结果
解决：
1. 检查麦克风是否连接并工作
2. 确认浏览器支持 Web Speech API（用 Chrome/Edge 试试）
3. 检查网络连接
4. 查看控制台日志（F12 → Console）
```

### 问题 2：摄像头无法启动
```
症状：点击"图像"后，日志显示 ⚠ 无法访问摄像头
解决：
1. 确认摄像头硬件正常（系统设置检查）
2. 检查浏览器是否被允许访问摄像头
3. 关闭占用摄像头的其他应用（如 Zoom、Teams）
4. 重启浏览器或系统
```

### 问题 3：输入框中的文本不显示
```
症状：说话或输入后，输入框仍为空
解决：
1. 确保点击了对应模态标签
2. 对于语音：等待识别完成后再检查
3. 查看控制台日志确认模态切换状态
```

---

## 📊 **性能指标**

| 模态 | 延迟 | 准确率 | 推荐场景 |
|------|------|--------|---------|
| 文本 | 0ms | 100% | 精确控制 |
| 语音 | 2-3s | 85-95% | 自然交互 |
| 图像 | 1-5s（待实现） | 待定 | 计算机视觉驱动 |

---

## 🚀 **高级用法**

### 组合多模态
1. 打开语音模态
2. 对着摄像头说出描述（例如："看起来像商业区，生成高密度建筑"）
3. 后端可融合语音识别结果 + 图像特征生成更精准的内容

### 开发者：扩展新模态
编辑 `/public/main.js`：
```javascript
// 添加新模态处理函数
async function startNewModality() {
    log("🔮 新模态已激活");
    // 实现自定义逻辑
}

// 在模态标签事件处理中添加条件
if (index === 3) { // 新模态
    currentModality = 'custom';
    startNewModality();
}
```

---

## 📝 **API 调用格式**

系统发送的 POST 请求格式：
```json
{
  "intent": "用户输入的意图文本",
  "modality": "text|voice|image",
  "constraints": {
    "density": "low|high"
  }
}
```

后端响应格式：
```json
{
  "status": "success",
  "compute_node": "Domestic-NPU-Cluster-01",
  "latency": 542,
  "data": {
    "tokens_used": 1024,
    "model_version": "MLLM-City-v1.0",
    "structures": [...]
  }
}
```

---

## 📞 **反馈与支持**

遇到问题或有改进建议？
1. 查看浏览器控制台日志（F12 → Console）
2. 检查本指南的故障排查部分
3. 报告 Issue 或联系开发者

---

**祝您使用愉快！🎉**
