# 🎮 面板操作快速参考卡

## 三秒速查

### 收起面板 📌
```
点击: ⟨
结果: 面板变成 50×50 icon, 3D 全屏
恢复: 点击 ⟩ 或点击 ↗
```

### 展开面板 🔓
```
点击: ⟩
结果: 面板恢复到上次的大小和位置
自动: 之前的所有自定义都会复原
```

### 移动面板 🖱️
```
拖动: 面板头部（标题区域）
结果: 面板跟随鼠标移动
保存: 新位置会被记住
```

### 调整大小 📏
```
拖动: 右下角 ↗ 把手
方向: →↓ 变大, ←↑ 变小
范围: 240-800px 宽, 150-600px 高
保存: 新尺寸会被记住
```

---

## 操作快速表

```
┌─────────────┬──────────────┬──────────────────┐
│   操作      │   位置/按钮  │   结果/效果      │
├─────────────┼──────────────┼──────────────────┤
│ 收起        │ 点击 ⟨       │ 隐藏内容 50×50   │
│ 展开        │ 点击 ⟩ 或 ↗  │ 显示内容恢复大小 │
│ 移动        │ 拖头部       │ 改变屏幕位置     │
│ 缩放大      │ 拖 ↗ 向 →↓   │ 面板变大         │
│ 缩放小      │ 拖 ↗ 向 ←↑   │ 面板变小         │
└─────────────┴──────────────┴──────────────────┘
```

---

## 按钮位置图示

```
面板展开时：
┌───────────────────────┐
│ 标题 [icon] ⟨◀ 收起按 │ ← 拖动这里移动
├───────────────────────┤
│                       │
│   内容显示区          │
│   (生成指令/日志等)   │
│                       │
│                    ↗  │ ← 拖动把手调大小
└────────────────────↗──┘
```

```
面板收起时：
┌──┐
│⟩ │ ← 点击展开
│  │
└─────↗─┘
   ↗ ← 可用于调大小
```

---

## 常用操作流程

### 流程 1：最大化 3D 视野
```
① 点击 ⟨ 收起
   ↓
② 面板变成小 icon
   ↓
③ 3D 城市占满屏幕
   ↓
④ 需要时: 点 ↗ 把手快速展开
```

### 流程 2：自定义工作区
```
① 拖 ↗ 把手调到喜欢的大小
② 拖头部移到最顺手的位置
③ 点 ⟨ 收起
④ 下次打开时，自动恢复这个布局
```

### 流程 3：演示设置
```
① 先调整好面板大小和位置
② 点 ⟨ 收起进入演示模式
③ 演示过程中可以点 ↗ 快速调参
④ 需要完整面板时点 ⟩ 展开
```

---

## 状态对照表

| 当前状态 | 按钮 | 可操作 | 快捷方式 |
|---------|------|--------|---------|
| 🟢 展开 | ⟨ | 所有 | 拖头部、拖把手 |
| 🔵 收起 | ⟩ | 把手、展开 | 点把手快速展 |
| 🟡 拖动 | - | 继续拖 | 松开完成 |
| 🟠 缩放 | - | 继续拖 | 松开完成 |

---

## 智能功能特性

### 自动记忆 💾
- ✅ 每次缩放都被保存
- ✅ 每次移动都被保存
- ✅ 收起/展开时参数不丢失
- ✅ 刷新页面还能恢复

### 安全保护 🛡️
- ✅ 最小宽度: 240px（防止看不清）
- ✅ 最大宽度: 800px（防止太夸张）
- ✅ 最小高度: 150px（防止压扁）
- ✅ 最大高度: 600px（防止超屏）

### 视觉反馈 👁️
- ✅ 把手悬停时发光
- ✅ 拖动时半透明
- ✅ 类名切换显示状态
- ✅ 平滑过渡动画

---

## 键盘快速访问

如果需要用键盘控制（需在浏览器控制台执行）:

```javascript
// 收起面板
document.getElementById('panel-toggle').click();

// 展开面板
document.getElementById('panel-toggle').click();

// 重置到默认大小
document.querySelector('.gui-panel').style.width = '320px';
document.querySelector('.gui-panel').style.height = '400px';

// 移除收起状态
document.querySelector('.gui-panel').classList.remove('collapsed');

// 获取当前尺寸
document.querySelector('.gui-panel').getBoundingClientRect();
```

---

## 故障排查 🔧

### 问题: 看不到把手
**解决**: 把手 ↗ 总是在右下角外侧，绿色发光，放大查看

### 问题: 面板太小看不清
**解决**: 拖 ↗ 把手向右下角移动放大，或点 ⟩ 展开

### 问题: 面板跑出屏幕
**解决**: 点 ↗ 把手会自动找到，点后可以继续拖动

### 问题: 想回到初始状态
**解决**: F12 打开控制台，执行:
```javascript
document.querySelector('.gui-panel').style.width = '320px';
document.querySelector('.gui-panel').style.height = '400px';
document.querySelector('.gui-panel').classList.remove('collapsed');
```

---

## 推荐的尺寸组合

| 用途 | 宽度 | 高度 | 位置 |
|------|------|------|------|
| 标准 | 320 | 400 | (20, 20) |
| 宽屏 | 500 | 400 | (20, 20) |
| 竖屏 | 240 | 500 | (20, 20) |
| 最小 | 240 | 150 | 右侧/底部 |
| 最大 | 800 | 600 | 任意 |

---

## 实时提示文本

当你操作时，看到的提示信息:

- 📏 开始缩放面板 - 表示系统开始追踪尺寸变化
- ✅ 缩放完成 - 表示新尺寸已保存
- 💾 面板已收起 - 表示状态已保存
- ✨ 面板已展开 - 表示状态已恢复

---

## 就这么简单！

```
收起  → 点⟨ → 得到全屏 3D
展开  → 点⟩ → 回到之前大小
移动  → 拖头部 → 放在最舒服位置
缩放  → 拖↗ → 调到喜欢的大小
恢复  → 都会自动记住! 💾
```

**现在你已经掌握了所有技巧！🎊**
